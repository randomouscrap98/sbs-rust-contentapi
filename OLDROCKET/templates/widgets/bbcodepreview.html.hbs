<!DOCTYPE html>
<html lang="{{language_code}}">
<head>
    {{> partials/basicmeta }}
    <title>SmileBASIC Source BBCode Preview</title>
    <meta name="description" content="Show the rendered bbcode">
    <!-- Keep in mind how many places may want the default colors, basic classes, etc. 
        We may need a css for the colors and classes, and another for layout structure
        with forms and stuff. Oh wait, there it is: base.css -->
    {{stylesheet "/base.css"}}
    {{script "/base.js"}}
    <style>
        html {
            height: 100%;
        }
        /* Since style is JUST for this widget, just include it directly */
        body {
            width: 100%;
            height: 100%;
            /* Let the parent figure out the padding on their side */
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            overflow-x: hidden;
            overflow-y: hidden;
            background-color: var(--bg_section);
            font-family: var(--font);
        }
        .content {
            display: block;
            width: 100%;
        }
        form {
            display: flex;
            flex-direction: column;
        }
        textarea { 
            flex: auto; 
        }
    </style>
</head>
<body>
{{#if result}}
<div class="content bbcode">{{{bbcode text}}}</div>
{{else}}
<form {{selfpost}}>
    <textarea placeholder="Enter text to test here" name="text"></textarea>
    <input type="submit" value="Test">
</form>
{{/if}}
</body>
</html>